# MobileShowcase — відкат до стану після 1-ї ітерації

Ідея з великим прямокутником скасована. Другу ітерацію (спільний вхід телефона + смуги з
лівого нижнього кута, збільшена смуга, гармонізована палітра) відкочено **повністю** —
`components/sections/MobileShowcase.tsx` повернувся до того вигляду, який був після першого
заходу.

---

## Що прибрано

| Що | Стан зараз |
|---|---|
| Смуга-прямокутник `200vw × 30vh` на всю сцену | знову тонка стрічка `116% × 88px` за списком |
| Вхідна анімація смуги (`x −680`, `y +190`, opacity) | немає — смуга просто на місці, як було |
| Вхідний зсув телефона (`x −240`, `y +140`) | немає — тільки `y 90 → 0` |
| `rotateY −24°` (нахил «за напрямком руху») | назад `+26° → 0°` |
| Stagger 0.28 / 0.34 між смугою й телефоном | немає, одне вікно `0 → 0.34` |
| Гармонізована палітра (`harmonize/bandFill/bandCore`) | назад статичний градієнт фіолет→індиго→ціан |
| `mix-blend-mode: screen` | прибрано |
| Перефарбування смуги під активний пункт | прибрано — смуга знову статична |
| Шар `.ms-band-enter`, `.ms-band-fill`, `.ms-band-core` | прибрано, знову `.ms-band-outer > .ms-band` + `::after` |
| Десктопні оверрайди смуги в `@media (min-width: 1024px)` | прибрано |

Константи `ENTER_*`, `BAND_*` і функції `harmonize()` / `bandFill()` / `bandCore()` видалені
повністю — мертвого коду не лишилось (перевірено grep-ом).

## Що працює як і раніше

- Sticky-пін `340vh` + `100vh` сцена, тільки від 1024px.
- Телефон: scroll-linked виїзд `rotateY 26° → 0°`, `rotateX 13° → 0°`, `y 90 → 0`,
  `scale 0.9 → 1`, fade; далі дрейф `0.34 → 1` до `−7° / 2°`.
- 4 пункти перемикаються по рівних відрізках прогресу (вікно `0.30 → 0.96`).
- Смуга: паралакс від скролу всієї сторінки + «удари» при зміні пункту + пружинне
  під'їжджання до активного рядка.
- Німб телефона, риска, номер і `text-shadow` заголовка перефарбовуються під активний пункт
  (це працювало й до другої ітерації — саме воно, а не смуга).
- Мобілка без піна, `prefers-reduced-motion` зі статичним фінальним станом.

---

## Перевірка після відкату

`npm run build` — успішно. Далі `npm run start` (продакшн-збірка) + headless Chrome.

| Перевірка | desktop | mobile | reduced-motion |
|---|---|---|---|
| Console errors / warnings / hydration | **порожньо** | **порожньо** | **порожньо** |
| Пін | `sticky` | `static` | `static` |
| Горизонтальне переповнення | 0px | 0px | 0px |
| Активний пункт по відрізках | `1/0.36/0.36/0.36` → `0.36/1/0.36/0.36` → `…/1` | усі 1.00 | усі 1.00 |
| Смуга (габарит на 1440) | `456 … 1252` | `−29 … 375` | `537 … 1352` (h 88, без rotate) |

Телефон на вході — знову без горизонтального зсуву:

| progress | transform |
|---|---|
| 0% | `translateY(90px) scale(0.9) rotateX(13deg) rotateY(26deg)`, opacity 0 |
| 15% | `translateY(50px) scale(0.944) rotateX(7.3deg) rotateY(14.5deg)`, opacity 1 |
| 34% | `none` (залишкові <0.05px) |

---

## Файли

| Файл | Що з ним |
|---|---|
| `components/sections/MobileShowcase.tsx` | єдиний змінений файл |

Решта проєкту не чіпалась. Зміни з **першої** ітерації лишаються на місці:
`app/page.tsx` (імпорт `MobileShowcase`), `app/globals.css`
(`body: overflow-x: hidden → clip`, без цього не працює sticky), видалений
`components/sections/About.tsx`, `public/phone/iphone-frame.png`.

---

## Якщо захочете повернутись до чогось із відкоченого

Не все з другої ітерації було про прямокутник — ці шматки можна підняти окремо, без нього:

- **Гармонізація кольору** смуги (приглушені тони + `screen`) — робить її менш «плакатною»
  на тому ж тонкому розмірі.
- **Перефарбування смуги** під активний пункт — зараз смуга статична, а німб і риска
  міняють колір; можна їх узгодити.
- **Вхід телефона зі зсувом** (без будь-яких змін смуги) — просто `x` на вході.
